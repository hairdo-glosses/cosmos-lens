# cosmos-lens
Cosmos Lens is a streamlined, read-only tool that enables developers to inspect the Base Sepolia network. It allows quick validation of network configuration, wallet balances, contract deployments, and provides testnet verification without modifying blockchain state
