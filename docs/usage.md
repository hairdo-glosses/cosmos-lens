# Cosmos Lens — Usage Guide

Cosmos Lens provides a lightweight, read-only solution for monitoring and inspecting **Cosmos-based networks**. This document explains how to use Cosmos Lens, from installation to executing network queries.

---

## Prerequisites

1. **Node.js** (version 14 or higher)  
2. **npm** (Node Package Manager) or **yarn** (optional)

Make sure you have these installed on your machine before continuing.

---

## Installation

To set up **Cosmos Lens**, you can install the necessary dependencies using either npm or yarn:

### Using npm:
```bash
npm install
